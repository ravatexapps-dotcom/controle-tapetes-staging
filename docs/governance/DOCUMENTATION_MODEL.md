# Documentation Model — Historical Compatibility Reference

STATUS: PASSIVE_HISTORICAL_COMPATIBILITY

This document no longer owns active documentation organization, update rules,
bootstrap behavior, classification, current state, or authorization.

Active operational and proportional-documentation rules are owned by
`docs/governance/AGENT_INSTRUCTIONS.md`. Current operational continuity is owned
by `docs/governance/current-state.json`. The catalog and
`docs/DOCUMENTATION_INDEX.md` are passive inventory surfaces outside the fixed
bootstrap.

The pre-compaction model, including its ownership tables, update matrices,
historical phase notes, language appendix, and structured-authority machinery,
remains available through Git history:

`git log --follow -- docs/governance/DOCUMENTATION_MODEL.md`

Accepted historical context remains in the append-only
`docs/ledgers/G28_LEDGER.md`. Consult only the task-relevant historical entry;
neither Git history nor the ledger overrides the active owners above.
