# Supervision Protocol — Historical Compatibility Reference

STATUS: PASSIVE_HISTORICAL_COMPATIBILITY

This document no longer owns active roles, executor-order requirements, gates,
evidence rules, authorization boundaries, or onboarding behavior.

Current supervision and execution rules are owned exclusively by
`docs/governance/AGENT_INSTRUCTIONS.md`. Current operational continuity is owned
by `docs/governance/current-state.json`; `AGENT_HANDOFF.md` is its generated
continuation view with no independent authority.

The former role definitions, order template, gates, and supervision handoff
appendix remain available through Git history:

`git log --follow -- docs/governance/SUPERVISION_PROTOCOL.md`

Accepted historical context remains in the append-only
`docs/ledgers/G28_LEDGER.md`. These historical sources do not authorize work or
compete with the active owners above.
